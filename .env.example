# Root-level infrastructure config, consumed by docker-compose.yml only.
# Application secrets live in back-end/.env and front-end/.env.local.
# Copy to .env and adjust if you need non-default values.

MEILI_MASTER_KEY=haestore_dev_master_key_change_me
